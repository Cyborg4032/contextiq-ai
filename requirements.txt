# ── ContextIQ ── requirements.txt ──────────────────────────────────
# Python 3.11 (see runtime.txt)
# Trimmed to only what app.py / main.py / core / utils actually import.
# Removed: openai-whisper, torch, torchaudio, deep-translator, reportlab,
#          fpdf2, streamlit-extras, tiktoken, ffmpeg-python
#          -> none of these are imported anywhere in the codebase, and
#             torch/whisper alone add 2-3GB+, which is almost certainly
#             why builds were failing/timing out on Streamlit Cloud.
# Added:   groq, langchain-chroma
#          -> imported directly (core/transcriber.py, core/vector_store.py)
#             but were missing from this file entirely, so any clean
#             install (any host) would crash on import before the app
#             even starts.

# ── Audio / Video acquisition ──────────────────────────────────────
yt-dlp>=2024.4.9
pydub>=0.25.1

# ── Speech-to-Text (remote APIs) ───────────────────────────────────
groq>=1.2.0                       # Groq Whisper API client (English)
requests>=2.32.0                  # Sarvam AI STT (Hinglish) via raw HTTP

# ── LLM Orchestration — LangChain LCEL + Mistral ───────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-mistralai>=0.1.0
mistralai>=0.4.0

# ── RAG Pipeline ────────────────────────────────────────────────────
langchain-chroma>=0.1.4           # Chroma wrapper used in core/vector_store.py
chromadb>=0.5.0
sentence-transformers>=3.0.0
langchain-huggingface>=0.0.3
huggingface-hub>=0.23.0

# ── Streamlit UI ────────────────────────────────────────────────────
streamlit>=1.35.0
watchdog>=4.0.0

# ── Utilities ───────────────────────────────────────────────────────
python-dotenv>=1.0.0
numpy>=1.26.0
tqdm>=4.66.0